# REQ-2026-0009：本地日常运行与恢复

- 需求版本：1.5.0
- 状态：已批准；Daily Ops 与引用数据日更已实施；WP-0047 的实时会话运行恢复、
  分钟封存和既有批准界面收口已获实施授权，当前尚未完成
- 初始批准日期：2026-07-29
- 来源：2026-07-27 WP-0007 拆分草案；2026-07-28 用户要求完成安全模拟盘前置需求
  决策；2026-07-29 用户批准日频与秒级行情产品化、前端承接和候选级按需日内研究
- 目标阶段：阶段 2、4、7
- 实现状态：主要本地闭环已实施；WP-0015 已完成显式日度窗口与备份恢复基线，
  WP-0023A 已完成真实仓库外备份、隔离恢复、单实例离线守护和八类故障演练，
  WP-0025 v1.1 已完成 L1/L2、研究股票输入、不可变快照、状态与恢复，WP-0029 已
  完成最新提交到本地决策链和安全预检，WP-0030 v1.1 已完成统一入口、运行摘要和
  同一身份恢复；WP-0031/0032 已完成本地调度、错过窗口恢复、系统页与异常收件箱，
  Windows 计划任务曾安装但当前运行失败；v1.5 由 WP-0047 承接实时会话、状态、
  分钟恢复与封存
- UI：UI-PROP-0008 v0.1 已批准并由 WP-0020 实现；WP-0047 复用已批准的
  UI-PROP-0001 v0.7，不改变导航或信息层级
- 追踪：见[需求索引唯一追踪表](./README.md#唯一追踪表)

## 目标

让单用户本地系统可以按日运行、发现陈旧或失败状态、从中断恢复并重建关键产物，同时
保持操作简单，不引入机构式发布和审批系统。

## 范围内

- 数据更新、快照发布、研究、Shadow 和后续模型作业的幂等调度；
- MiniQMT 实时行情会话、午休、重连、收盘封存、缺口处置和跨日清场；
- 不可变输入、幂等键、租约、心跳、有界重试、进度和失败原因；
- 进程重启、数据中断、提供方限流和磁盘故障后的安全恢复；
- 快照、SQLite、配置、模型及未来授权记录的备份和恢复演练；
- 结构化脱敏日志、健康、新鲜度和紧凑异常视图；
- 日更、查询、局部回测和预测性能基线；
- 本地操作、事故处理和数据重建指南。

## 范围外

- 多用户、远程托管或企业调度平台；
- 第二套数据真相或第二个执行引擎；
- 自动越过失败关闭状态；
- 未批准的 MiniQMT 账户或交易连接；
- UI-PROP-0008 获批前实现对应业务页面。
- 以无限重启掩盖实时源持续故障，或用临时行情绕过日度原子提交。

## 功能要求

1. 长历史重建和模型训练不得隐藏在 API 请求内。
2. 每个作业必须固定输入身份并支持幂等重试。
3. 未完成发布不得更新当前快照指针。
4. 陈旧、断开、阻断和错误必须可区分，并给出恢复动作。
5. 日志和健康接口不得包含 Token、账户标识、真实行情值或订单载荷。
6. 恢复演练必须证明关键投影可由不可变产物或事件重建。
7. 实时行情会话必须独立于日度调度运行，但共用交易日历、数据身份、异常入口和
   单实例纪律。
8. 盘中必须区分 `preopen / streaming / lunch_break / stale / disconnected`；
   收盘后必须区分 `sealing / reconciled / blocked`。
9. 重连必须创建新会话并记录缺口区间；不得把重连后的最后值拼接成无缺口连续会话。
10. 原始实时数据清理前必须验证分钟/秒级长期聚合、会话清单、内容哈希和删除台账；
    验证失败保留数据并进入 `recovery_required`。
11. 只有收盘封存、覆盖和日线对账成功后才可推进新的完成日快照；失败时保留上一完整
    指针，盘中临时投影不得成为恢复替代品。
12. 计划任务状态必须使用 `installed / not_installed / query_failed` 三态；查询失败
    时保留最后已知安装事实和错误证据，不能把 Windows/WSL 互操作故障解释为任务缺失。
13. 每次服务运行必须把启动、订阅、重试、终止根因和最后成功心跳写入脱敏运行证据；
    计划任务标准输出和错误输出必须可诊断并有界轮转，空日志不能算可运营。
14. 实时服务恢复必须幂等：重复启动、进程中断和同日重启不能重复发布一分钟主键；
    收盘封存前的形成中分钟保持可变投影，封存后发布内容寻址制品和覆盖/缺口清单。

## 已批准运行基线

1. 时区固定为 `Asia/Shanghai`；交易日以版本化交易日历和提供方完成状态共同判断。
2. 盘后增量数据、快照和决策流水线安排在 16:30 后；只有提供方确认最近完成交易日且
   所需数据集完整时才能推进目标与计划。
   龙虎榜/席位存在保守的 20:05 完成门；16:30 可以启动并核验其他数据，但事件尚未
   到窗时保持 `waiting_provider`，不得提交缺少目标日事件的生产快照。
3. 次交易日 08:30～09:20 用于恢复、行情握手和未来 Paper 启动对账；失败时当天券商
   动作保持阻断。
4. 长历史回填、完整封存回放和模型训练继续使用显式长任务，不进入日度关键路径。
5. 日度增量流水线超过 30 分钟进入 `stale` 或 `blocked` 并提示恢复，不以无限重试
   掩盖问题。
6. 备份目录由 `ASTRAMIND_BACKUP_DIR` 指向仓库和 `var/` 之外的位置；未配置或验证
   失败时，研究和 Shadow 可继续，但 Paper 写入保持阻断。
7. 每次追加式迁移前、首个券商写入前和每个完成交易日结束后建立备份；保留最近 30 个
   日度恢复点和 12 个自然月月末恢复点。
8. 不可变执行事件以券商重查和事件重放实现 RPO 0；本地投影允许重建，不把投影文件
   当作唯一事实。恢复目标为 30 分钟内回到可判断的健康或明确阻断状态。
9. UI-PROP-0008 未批准不阻塞后端和命令行实现；对应页面继续禁止实施。
10. 每个完成交易日的统一数据提交必须同时刷新证券主表、历史名称/ST 区间、公司行动、
    SW2021 行业分类注册表和行业成员关系；不得把上一快照中的身份数据静默沿用为当日已
    核验结果。
11. 证券主表、行业分类和成员关系采用当日完整提供方视图对账；名称/ST 与公司行动按
    实际公告/可用时间合并。任一关键引用数据请求失败、达到提供方上限、身份冲突或点时
    语义不明确时，保留上一完整快照并进入 `waiting_provider`、`blocked` 或
    `recovery_required`。
12. 实时服务在开盘前只做会话和参考数据预检，午休不误判为断线，收盘后停止形成中
    投影并进入封存；跨日恢复不得沿用上一交易日会话身份。
13. 候选级分钟/Tick 长任务可以按冻结请求幂等恢复，但不能隐藏在页面请求内，也不能
    阻塞完成日市场页和既有日频研究。

## 验收标准

Given 作业在发布前中断，When 使用相同幂等键恢复，Then 不产生冲突版本且当前指针仍
指向最后一个完整版本。

Given 提供方限流或断开，When 重试预算耗尽，Then 作业进入可见失败状态并保留安全恢复
入口，不循环无限重试。

Given SQLite 投影丢失但事件和备份可用，When 执行恢复演练，Then 重建结果与恢复前身份
一致。

Given 查看日志、健康页和错误摘要，When 执行密钥扫描，Then 不包含 Token、账户标识或
真实订单内容。

Given 行情在午休期间没有更新，When 运行健康判断，Then 状态是 `lunch_break` 而不是
误报 `disconnected`；午后恢复使用同一合法会话或显式新会话身份。

Given 实时桥重启，When 新消息到达，Then 系统发布新会话、前一会话终点和已知缺口，
不会把两段数据伪装成无缝连续。

Given 收盘封存或日线对账失败，When 日度调度准备切换当前指针，Then 保留上一完成日，
显示 `blocked` 和恢复动作，盘中临时值不被提升为正式日线。

Given 候选级日内补拉中断，When 使用相同冻结请求恢复，Then 已完成候选不会重复产生
冲突版本，候选全集和参数不改变，页面请求不会承担长任务。

## WP-0015 实施结果

- 16:30～17:00 盘后与次交易日 08:30～09:20 盘前窗口已形成确定性决策；
- 提供方未完成、日度预算超时、备份未就绪和恢复失败分别输出明确阻断；
- SQLite 使用在线 Backup API 形成一致性副本，当前数据指针以相对路径和哈希保存；
- 备份清单不包含绝对路径、Token、账户标识或订单载荷；
- 隔离恢复核对文件哈希、SQLite 完整性和 Shadow 事件计数，不覆盖当前 `var/`；
- WP-0023A 已在私有 `.env.local` 配置真实仓库外目录并完成部署验收；
- 最新恢复点验证22个文件、4个 SQLite 和4条 Shadow 事件，状态为
  `paper_recovery_ready=true`，但不推导券商授权。

## WP-0023A 实施结果

- 独立 SQLite/WAL 控制台账保存30秒单实例租约、不可变任务检查点和演练报告；
- 守护核验备份恢复及
  `DataSnapshot → FeatureSnapshot → PredictionBatch → PortfolioTarget → OrderPlan`
  身份链，最终 Paper dispatch 永久禁用；
- 中断后复用检查点，重复运行返回相同结果，不重复任务；
- 陈旧数据、外来挂单、Mandate 过期、未知提交、回调乱序、重复实例和控制库故障均
  失败关闭；
- 八类故障演练全部通过，MiniQMT 连接与券商写入尝试均为0。

## WP-0025 / WP-0029 实施结果

- 16:30 后由一个本地作业增量更新交易日历和行业指数日线；
- 依次发布不可变数据集、`DataSnapshot` 与 `MarketRotationSnapshot`，全部完成后才
  原子更新当前指针；
- 页面和功能模块只读消费数据管线，不在请求处理路径各自联网补数；
- 提供方未完成、关键缺口、中断或重试耗尽时保留上一完整快照，并显示
  `waiting_provider / stale / blocked / recovery_required`；
- [WP-0025 v1.1](../planning/work-packages/WP-0025-daily-data-derived-snapshot-orchestration.md)
  已完成真实只读联网、合成中断恢复和幂等重跑；2026-07-28 当日提供方数据为空时
  正确保留 `waiting_provider`，没有伪造成功；
- [WP-0029](../planning/work-packages/WP-0029-daily-research-decision-chain.md)已把最近
  完整提交编排为研究薄腰和 Local Replay/Paper 就绪预检；现有 Paper dispatch
  永久禁用，未来活动 Paper 迁移必须使用新的准确工作包与授权。
- Daily Ops 历史日期恢复按冻结基础快照和目标日期解析准确数据运行与不可变提交；
  更新日期已存在时不得读取全局 latest 代替历史运行，准确数据步骤已经完成时恢复
  直接复用，不重新抓取提供方。

## 仍受保护

- `ASTRAMIND_BACKUP_DIR` 的本机实际路径属于部署配置，不写入仓库；
- 超出已批准布局的可写运行界面仍需新的 UI 提案；
- 本需求不授权 MiniQMT 模拟盘下单、撤单、常设授权或 Live。

## Daily Ops 1.0 实施结果

Daily Ops 1.0 把日常自动运行拆为三个连续里程碑：

1. [WP-0030](../planning/work-packages/WP-0030-unified-daily-run-orchestrator.md)：统一数据、
   决策与运行摘要；
2. [WP-0031](../planning/work-packages/WP-0031-local-daily-scheduler-recovery.md)：本机调度、
   错过窗口补跑与启动恢复；
3. [WP-0032](../planning/work-packages/WP-0032-daily-operations-ui-acceptance.md)：系统页、
   异常收件箱与正式验收。

WP-0030/0031/0032 均已完成；准确 Windows 计划任务已经用户确认并安装。该完成状态
不授权任何券商动作，也不表示 v1.4 的实时会话、收盘封存和候选级长任务已实现。

## 版本历史

| 版本 | 日期 | 变更 | 状态 |
| --- | --- | --- | --- |
| 1.0.0 | 2026-07-27 | 从 REQ-0001 规划基线拆分本地运行与恢复草案 | 草案 |
| 1.1.0 | 2026-07-28 | 冻结日度窗口、任务预算、备份保留和恢复目标；UI 与券商动作仍独立受保护 | 已批准 |
| 1.2.0 | 2026-07-28 | 日度快照纳入目标日事件数据，并增加 20:05 保守完成门与长历史独立任务边界 | 已批准并实现 |
| 1.3.0 | 2026-07-29 | 用户要求把证券主表、名称/ST、公司行动、行业分类与成员关系纳入同一日度原子更新 | 已批准并实现；真实只读验收通过 |
| 1.4.0 | 2026-07-29 | 增加实时行情会话状态、午休/重连、收盘封存、清理台账和候选级日内长任务恢复 | 已批准需求；实时运行与新 UI 未授权实施 |
| 1.5.0 | 2026-07-30 | 用户插队授权 WP-0047 恢复只读实时服务、补齐运行证据、分钟暖启动/封存与既有批准多周期界面 | 已批准实施；不含账户、Paper/Live 或订单 |
