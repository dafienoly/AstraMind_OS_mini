# REQ-2026-0005：短线研究证据与策略晋级

- 需求版本：1.1.0
- 状态：已批准，已实现
- 来源：2026-07-27 用户明确要求评审、批准并实施 REQ-2026-0005
- 目标阶段：阶段 2D、3、4B
- 实现状态：WP-0008 已完成事件快照、九版本封存证据与手动晋级账本；用户已将
  反转/量价 10 日版明确晋级为纯本地 Shadow 诊断挑战者；WP-0013 已用该准确版本
  生成首个生产 `FeatureSnapshot` 与 `PredictionBatch`
- UI：UI-PROP-0004 v0.2 已批准；未来 UI 工作包仍须引用准确版本
- 追踪：见[需求索引唯一追踪表](./README.md#唯一追踪表)

## v1.1 连续事件数据扩展

2026-07-28 用户明确批准：先把龙虎榜、席位和股东户数延伸到 2026 并纳入唯一日度
`DataSnapshot`，再可恢复回填 2005–2022 历史，最后独立核验 2000–2006 涨跌停覆盖。

- 2026 事件增量属于当前观察输入，不改变 2023–2025 封存窗口或既有证据身份；
- 2005–2022 历史只形成新的数据版本，不得用于回头修改已冻结策略后仍沿用旧证据；
- 龙虎榜和席位按 Tushare 实际可得历史回填；股东户数允许早期空覆盖，但必须保存
  请求证据并按公告可用时间使用；
- 日更提交只在三个事件数据集与目标日其他数据共同形成一个准确快照后完成；
- 早期涨跌停探测不属于策略证据，也不能根据固定百分比伪造价格。

### 新增验收

Given 目标日事件接口已完成，When 日度数据管线提交，Then 同一 `DataSnapshot` 包含
目标日行情、行业和三个准确事件数据集版本。

Given 2005–2022 回填中断，When 使用相同基础事件版本和区间恢复，Then 已完成请求不
重复，既有 2023–2026 观察不丢失且同名数据集不重复。

Given 股东户数早期窗口没有记录，When 发布覆盖，Then 保留空响应事实，不把缺失填零，
也不把报告期当作公告可用时间。

## 版本历史

| 版本 | 日期 | 变更 | 状态 |
| --- | --- | --- | --- |
| 1.0.0 | 2026-07-27 | 冻结事件数据、2023–2025 封存证据和手动晋级 | 已批准并实现 |
| 1.1.0 | 2026-07-28 | 批准 2026 日更事件、2005–2022 历史连续化和早期涨跌停覆盖核验 | 已批准并实现 |

## 目标

使用一个不可变数据快照形成可重复的全市场开发期与封存期短线证据，让用户能够检查
失败场景并手动晋级一个明确策略版本，同时保持策略晋级与券商授权分离。

## 范围内

- 核验并生产化龙虎榜、股东户数等短线事件数据及其可用时间；
- 复用点时股票池、统一回测引擎和三类候选策略；
- 开发期只用于研究和冻结策略版本；
- 2023–2025 年封存区间只评价已冻结版本；
- 输出按年份、周期、市场状态、行业、容量和成本拆解的证据；
- 记录手动晋级或不晋级决定及其策略版本身份；
- 晋级后的策略只能产生短线 `PortfolioTarget`。

## 范围外

- 根据封存结果继续调参；
- 将龙虎榜盘后事实用于同日更早成交；
- 自动选择冠军；
- MiniQMT 账户读取、Paper、Live、常设授权或真实订单；
- 用当前行业归属或未来数据改写历史。

## 功能要求

1. 一次评价必须固定一个 `DataSnapshot`、成本版本、股票池版本和策略版本。
2. 龙虎榜与股东户数必须按照实际 `available_at` 进入特征。
3. 开发期与封存期的产物、参数和身份必须分离。
4. 所有候选必须使用相同评价区间、成本和可交易性约束。
5. 证据必须包含逐笔台账、拒绝原因、按年结果、容量和成本口径；市场状态或历史行业
   数据未形成点时版本时必须明确显示不可用，不得用当前分类补齐。
6. 晋级必须是显式用户决定，并记录被晋级的准确 `StrategyVersion`。
7. 晋级不得改变 MiniQMT、Paper 或 Live 状态。

## 验收标准

Given 一条公告晚于决策时点，When 构建历史特征，Then 该观察不得进入当时的
`FeatureSnapshot`。

Given 一个已经冻结的策略版本，When 运行 2023–2025 封存回放，Then 运行不得修改
参数，并能以同一输入重建相同证据身份。

Given 数据集存在关键缺口，When 缺口会影响候选比较，Then 该区间失败关闭并显示缺口，
不得用零值、当前值或相邻值补齐。

Given 用户晋级一个策略，When 形成晋级记录，Then 记录准确版本、证据身份和适用分仓，
且 `broker_enabled` 仍为 `false`。

## 批准决定

- 开发截止固定为 2022-12-31，封存窗口固定为 2023-01-01 至 2025-12-31；
- 三类家族各冻结 2／5／10 个交易日周期，共九个准确 `StrategyVersion`；
- 龙虎榜和席位使用 Tushare `top_list`／`top_inst`，股东户数使用
  `stk_holdernumber`；关键权限、发布或覆盖缺口失败关闭；
- 不设置自动收益率或排名晋级阈值。晋级只能由用户针对准确策略版本和证据身份作出
  显式决定，并记录理由；
- 历史区间在本需求批准前已经客观存在，不能倒推证明“从未看过”。封存纪律从候选
  版本冻结后生效，证据必须携带这一限制，不得宣称真正前瞻或 Live 表现；
- 历史行业归属和市场状态拆解分别由 REQ-2026-0008 提供；在其数据可用前，证据显式
  标记不可用，不阻塞价格量能策略的同口径回放，也不得伪造拆解结果。

## 授权边界

本批准只授权 Tushare 事件数据只读回填、本地全市场研究回放和本地晋级记录。它不授权
读取 MiniQMT 账户、连接交易端口、启用 Paper／Live、创建常设授权或产生真实订单。

## 实际晋级决定

2026-07-28，用户针对准确封存证据作出显式晋级决定：

- 策略版本：
  `strategy-version:e78f48cdb4a4f7040ff66aff54a0816a3a2130deca5ff935c1c7f6ae2f9a8585`；
- 证据包：
  `evidence-bundle:sha256:9fdff6510b2eb65328066d1d815216ca8d83b689b3ccfec408cdba46b5f2f95e`；
- 单项证据：
  `sealed-evidence:sha256:e8ae34d229e687ca47aed0a0a641cb4776ccbc525a62b84132f5bb5a425912f4`；
- 晋级决定：
  `promotion:sha256:b33487a4042356161ea03fd3a4bd9c2a980629feea5ad49239fa2ec563334afd`；
- 身份：反转/量价 10 日版，战术分仓纯本地 Shadow 诊断挑战者；
- 封存期总收益 7.385%，Sharpe 0.306、Calmar 0.043、最大回撤 58.811%，2023 年
  收益 -24.791%。

九个版本中只有该版本总收益为正，但风险调整后证据很弱。晋级用途仅为收集新的持续
Shadow 证据，不能称为冠军、有效策略或可交易策略，也不授权 Paper、Live、真实常设
授权或券商订单。`broker_enabled=false`。

首个晋级后生产预测批次为
`prediction-batch:095c3240a91e6df159d5b306cb6ab9cde979ab30d866c06503fd1edb93d50b31`，
引用 2026-07-27 最近完成交易日的特征快照；该事实只表示版本链已运行，不改变上述
弱证据评价。

WP-0029 已证明同一晋级决定可以从 WP-0025 的准确日度提交重复构建当前研究头：
`feature-snapshot:d8211bbf…87f1 → prediction-batch:5f3bd7f6…6f65`。该链不重新训练、
调参或晋级，不创建 Shadow 周期，也不改变策略弱证据评价。
