---
status: accepted
---

# ADR-0002：券商执行前必须跑通本地 Shadow

每个可执行策略必须先通过本地 Shadow 适配器，并使用未来 MiniQMT 同一套 `OrderPlan` 契约。策略可以手动晋级，不设固定等待期；但连接 MiniQMT 和启用真实资金始终是另外的明确决策。

这样既避免“永远观察、不允许前进”，也避免为实盘再造一条未经验证的执行路径。
