# WP-0005：首批三类短线候选

- 状态：已完成候选生成与统一回测接入
- 完成日期：2026-07-27
- UI：不新增页面

## 策略家族

- 事件/关注：异常成交额、显著日收益和涨跌停参与代理；
- 动量/突破：20 日价格突破与量能确认；
- 反转/量价：五日显著下跌后的正向反弹与非拥挤量能。

三类逻辑共用 `2/5/10` 个交易日周期、点时股票池、执行引擎和指标口径。每个家族可
冻结确定性的 `StrategyVersion`，候选可形成确定性的 `PredictionBatch` 身份。

## 重要限制

`top_list` 尚未进入当前生产快照，因此“事件/关注”当前只是行情代理，不得声称完成
龙虎榜策略。`make tactical-research-smoke` 已在当前生产快照上完成 10 只股票的小样本
管线贯通，输出位于 `var/research/`；该输出不是有效性、封存回放或晋级证据。
