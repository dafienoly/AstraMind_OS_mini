{
  "name": "astramind-os-mini",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "build": "pnpm --filter @astramind/web build",
    "lint": "pnpm --filter @astramind/web lint",
    "test": "pnpm --filter @astramind/web test",
    "typecheck": "pnpm --filter @astramind/web typecheck",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
